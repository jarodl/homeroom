<div id="new_user">
  <h2 class="header">New material</h2>

  <div class="content">
    <% form_for [:admin, @material], :html => { :multipart => true } do |f| %>
      <%= f.error_messages %>

      <p>
        <%= f.label :name %><br />
        <%= f.text_field :name %>
      </p>

      <p>
        <%= f.label :course %><br /> 
        <%= collection_select(:material, :course_id, Course.all, :id, :title) %>
      </p>

      <p>
        <%= f.label :file %><br />
        <%= f.file_field :upload %>
      </p>

      <p><%= f.submit "Add material", {:class => 'blank'} %></p>
    <% end %>
  </div>
</div>
