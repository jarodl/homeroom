<h2 class="header">Latest activity in your classes</h2>

<div class="content">
  <div id="courses">

    <% @courses.each do |course| %>
      <div class="course">
        <h3 class="header"><%= course.title %><span class="grey">- announcements</span></h3>

        <div class="course-day">
          <ul>
            <% course.assignments.each do |assignment| %>
              <li><%= assignment.name + ' is in ' + distance_of_time_in_words_to_now(assignment.due) %></li>
            <% end %>
          </ul>
        </div>
      </div>
    <% end %>

  </div>
  <!-- end courses -->
</div>
<!-- end content -->
